[112 tools called]

## 作業概要
- **データモデル刷新**  
  - `app/db/models/file.py` を v2 スキーマへ変更し、`blob_path` / `original_name` / 各メタ情報列（`application` など）へ再構成。  
  - `app/db/migrations/versions/bf9d9f4ffc23_files_v2_schema.py` を追加し、`files` & `file_references` を再作成する破壊的 migration を実装（`alembic upgrade head` コメント付）。
- **アップロード/Blob 処理の全面改修**  
  - `app/schemas/file.py`・`app/services/blob_service.py`・`app/api/v1/routes_files.py`・`app/services/file_service.py` を刷新。  
    - UUID を発行→`files/{uuid}.ext` 形式で保存、Blob メタデータに `application` などを埋め込み、DB には `blob_path` とメタ情報のみ保存。  
    - Azure Blob SDK を async 化し、SAS 発行やメタデータ付きアップロード／削除に対応。
- **検索を Azure AI Search 一本化**  
  - `app/services/search_service.py` を再実装し、`SearchClient` 経由で `content/application/...` を検索。  
  - `app/services/file_service.py` の `search` は Azure Search 専用にし、SQL LIKE を全廃。  
  - 新しい IaC スクリプト `infrastructure/search_setup.py` と再クロール用 `scripts/reindex_search.py` を追加。
- **関連サービス・ユーティリティ整備**  
  - `scripts/utils/filename_parser.py`/`tests/test_files.py` を新命名規則準拠へ更新。  
  - `scripts/ingest_existing_files.py` を v2 スキーマ対応に刷新。  
  - `app/services/dashboard_service.py` など周辺コードを新カラムへ追随、`app/core/config.py` では `SEARCH_BACKEND` デフォルトを `azure` に変更。
- **README 更新**  
  - 新しい `files` スキーマ／アップロード仕様／Azure Search セットアップ手順 (`infrastructure/search_setup.py` / `scripts/reindex_search.py`) を追記。  
  - `/files/search` のパラメータ・レスポンスを Azure Search ベースの内容へ差し替え。

## テスト
- 自動テストは未実行です。`alembic upgrade head` → `pytest` の実行と、`infrastructure/search_setup.py` / `scripts/reindex_search.py` による Azure Search セットアップ・再クロールをお願いします。